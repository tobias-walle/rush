# React with Redux Application

A starting template for react with redux

## Commands

To get started run:

```bash
npm install
npm start
```

To build the application run:

```bash
npm run build
```
